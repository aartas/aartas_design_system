import 'package:flutter/material.dart';

class TestingIt extends StatelessWidget {
  const TestingIt({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Good its working.");
  }
}
